upstream {{ name }} {
    server 127.0.0.1:{{ port }} max_fails=3 fail_timeout=30s;
}
server {
    listen          8000;
    server_name     {{ name }}.docker.openlabs.us;
 
    location / {
      proxy_pass      http://{{ name }};
    }
}
